1. Реалізував клас Order. Цей клас зберігатиме значення: id, date, cost, products

1.1 Описав цю сутність за допомогою JPA-анотацій.

2. Реалізував клас Product. Цей клас зберігатиме значення: id, name, cost

2.1 Описав цю сутність за допомогою JPA-анотацій.

3. Створив ProductRepository для взаємодії з сутністю Product

4. Замовлення будуть зберігатися в спеціалізованому класі-репозиторії OrderRepository

5. Сконфігурував Spring-додаток через application.yml

6.1 Додаток доступний за URL: http://localhost:8080

6.2 Налаштував логування на рівні INFO для пакетів програми та для пакету org.springframework.web

Процес логування включає виведення в консоль та запис у файл

7. Реалізував OrderService який буде взаємодіяти з OrderRepository та реалізовувати операції: отримання по id, отримання всіх, додавання, видалення .